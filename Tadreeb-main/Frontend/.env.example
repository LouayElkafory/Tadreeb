# Base URL of the FastAPI backend (used when VITE_USE_MOCK_API=false)
VITE_API_URL=http://localhost:8000

# When true, the frontend uses local mock responses instead of calling the backend.
# Set to false once the RAG backend is ready.
VITE_USE_MOCK_API=true
